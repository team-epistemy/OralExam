[pytest]
# Unit tests live in the tests/ sub-folder. `pythonpath = .` puts the
# backend package on sys.path so a bare `pytest` works (no PYTHONPATH needed).
testpaths = tests
pythonpath = .
addopts = -q
