# Test materials

Drop a subject **PDF** here (e.g. `operations.pdf`, `biology.pdf`). The filename
becomes the course name. Then run `python ../journey_e2e.py` (see `../testing.md`).

`sample-operations.pdf` is a bundled minimal sample. Your own PDFs are git-ignored.
