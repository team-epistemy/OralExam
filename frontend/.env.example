# Copy to `.env` for local development and fill in the value.
# Only VITE_-prefixed vars are exposed to the client bundle (and are public).

# Base URL of the backend API (no trailing slash).
# Local dev example:   http://localhost:8080
# Deployed example:    https://<your-backend-host>
VITE_API_URL=
